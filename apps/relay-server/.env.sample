# Pi Relay Server Configuration
# Copy this file to .env in your config directory and fill in the values.

# ============================================================================
# REQUIRED: Encryption key for secrets at rest (AES-256-GCM)
# ============================================================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
RELAY_ENCRYPTION_KEY=

# Key version for rotation support (increment when rotating keys)
RELAY_ENCRYPTION_KEY_VERSION=1

# ============================================================================
# Sandbox Provider
# ============================================================================
# Provider type: "mock" (default, for testing) or "docker" (production)
SANDBOX_PROVIDER=mock

# Docker image for sandbox containers (when using docker provider)
SANDBOX_DOCKER_IMAGE=pi-sandbox:local

# ============================================================================
# Optional: TLS (HTTPS)
# ============================================================================
# Provide both to enable HTTPS on the relay server directly.
# When running behind the Caddy dashboard proxy this is not needed.
# PI_RELAY_TLS_CERT=/path/to/cert.pem
# PI_RELAY_TLS_KEY=/path/to/key.pem

# ============================================================================
# Optional: Direct API keys (alternative to storing in database)
# ============================================================================
# These can also be managed via the dashboard at /settings
# If set here, they will be available to all sessions but won't be stored
# in the database. Database-stored secrets take precedence.

# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=AIza...
# GROQ_API_KEY=gsk_...
# DEEPSEEK_API_KEY=sk-...
# OPENROUTER_API_KEY=sk-or-v1-...
# GITHUB_TOKEN=ghp_...
