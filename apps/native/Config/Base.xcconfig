// Base settings shared across all configurations.

SWIFT_VERSION = 6.2
CODE_SIGN_STYLE = Automatic
DEVELOPMENT_TEAM =

// Bundle IDs (overridden by Local.xcconfig)
PI_BUNDLE_ID_MACOS = dev.placeholder.pi.macos
PI_BUNDLE_ID_IOS = dev.placeholder.pi.ios

// Include local overrides (optional -- won't fail if missing)
#include? "Local.xcconfig"
