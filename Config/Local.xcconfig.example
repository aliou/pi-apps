// =============================================================================
// LOCAL CONFIGURATION - Copy to Local.xcconfig
// =============================================================================
// This file is NOT committed to git. Each developer maintains their own copy.
// 
// To set up:
//   cp Config/Local.xcconfig.example Config/Local.xcconfig
//
// Then edit the values below.
// =============================================================================

// Bundle IDs - Use unique values to avoid conflicts with other developers
// Format suggestion: dev.<yourusername>.pi.desktop
PI_DESKTOP_BUNDLE_ID = dev.example.pi.desktop
PI_MOBILE_BUNDLE_ID = dev.example.pi.mobile

// Optional: Your Apple Developer Team ID (10-character alphanumeric)
// If set, Xcode won't prompt you to select a team
// DEVELOPMENT_TEAM = XXXXXXXXXX

// Optional: Specific code signing identity
// Usually not needed - Automatic signing handles this
// CODE_SIGN_IDENTITY = Apple Development
