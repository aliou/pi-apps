# Hoist esbuild-externalized transitive deps so require() resolves them at runtime.
# ssh2 (via gondolin) and cpu-features (via ssh2) have native bindings / CJS __dirname
# usage that prevents esbuild from bundling them.
public-hoist-pattern[]=ssh2
public-hoist-pattern[]=cpu-features
